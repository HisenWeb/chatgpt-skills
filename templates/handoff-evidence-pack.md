# 模板：Handoff Evidence Pack

适用场景：

- 生成 `handoff-current.md` 时，需要同步生成 `handoff-evidence.md`；
- 常规 handoff 或中转蒸馏 handoff 都适用；
- 需要把旧窗口 / 当前窗口中可迁移的证据摘录出来；
- 需要明确哪些原始材料不能自动迁移，必须让用户在新窗口重新上传或提供替代证据。

说明：

- 本模板是 `handoff-evidence.md` 的结构模板，不是独立 Skill。
- 使用时必须结合 `skills/04-handoff-regular.md` 或 `skills/04b-handoff-distillation.md`。
- 如果涉及 TodoList，TodoList 语义仍以 `skills/04a-goal-todolist.md` 为准。

复制使用：

```text
请生成 handoff-evidence.md，结构如下。

# Handoff Evidence Pack

## 1. 本文件用途

说明本文件用于帮助新窗口获得继续工作所需的证据材料。

必须明确：
- 本文件不是聊天摘要；
- 本文件不是原始附件替代品；
- 本文件只整理已在旧窗口 / 当前窗口中可见、可摘录、可说明来源的证据；
- 无法自动携带或无法复核的原始材料必须重新上传或提供替代证据。

## 2. 已整理进本文件的证据

### 2.1 用户已确认结论

每条结论使用以下格式：

- 结论：
- 来源：用户明确确认 / 多轮讨论后确认 / 当前窗口明确表达
- 对下一步的影响：
- 可信度：已确认 / 待复核

### 2.2 文件 / Prompt / Skill / 模板证据

每个材料使用以下格式：

- 材料名称：
- 类型：文件 / Prompt / Skill / Template / README / 索引 / 脚本
- 已读取范围：
- 关键摘录或摘要：
- 支撑的判断：
- 仍需复核：

### 2.3 执行端输出 / 日志 / 报错 / diff / 命令输出

每个材料使用以下格式：

- 材料名称：
- 原始摘录：
- 关键信息：
- 支撑的判断：
- 不确定点：
- 新窗口是否需要重新提供完整日志：是 / 否
- 原因：

### 2.4 图片 / 截图证据卡

每张关键图片使用以下格式：

#### 图片证据：图片用途或文件名

- 来源：旧窗口上传截图 / 当前窗口截图 / 用户粘贴图片 / 浏览器截图
- 用途：说明它用于证明什么问题
- 已识别关键信息：
  - 页面 / 工具 / 报错位置：
  - 可见文字：
  - UI 状态：
  - 异常现象：
- 已基于图片确认的结论：
- 不确定点：
- 新窗口是否必须重新上传原图：是 / 否
- 必须重新上传原因：
- 替代方式：

禁止只写“见截图”。
看不清、未识别、未确认的图片细节必须写入“不确定点”。

## 3. 原始材料定位卡

每个关键原始材料使用以下格式：

### 原始材料：材料名

- 类型：截图 / 图片 / zip / md / 源码包 / 日志 / diff / 本地文件 / GitHub 文件 / File Library 文件
- 旧窗口来源：用户上传附件 / 用户粘贴文本 / 用户提供本地路径 / GitHub 链接 / File Library / Project
- 原始文件名：
- 用户本地路径：未知 / 路径
- ChatGPT 新窗口是否能自动访问：不能假设
- 已摘录到本文件的内容：
- 是否必须重新上传原件：是 / 否
- 如果不重新上传会卡在哪里：
- 替代方式：重新上传 / 从 File Library 或 Project 选择 / 提供 GitHub 链接 / 让执行端读取后贴出结果 / 重新粘贴日志

## 4. 新窗口需要用户重新上传 / 补充的材料

用表格列出：

| 材料 | 类型 | 是否必须 | 原因 | 不提供的影响 | 替代方式 |
| --- | --- | --- | --- | --- | --- |
|  |  | 必须 / 建议 |  |  |  |

必须覆盖：
- 图片 / 截图原图；
- zip / 源码包 / 附件原件；
- 旧窗口上传过但新窗口未上传的文件；
- 用户本地路径指向的文件；
- 未完整读取过的文件；
- 执行端日志、命令输出、diff、测试结果；
- 需要视觉复核、代码复核或重新执行才能确认的材料。

## 5. 证据缺口 / 待复核

每个缺口使用以下格式：

- 缺口：
- 影响：
- 当前只能得出的保守结论：
- 建议补证方式：

## 6. 不需要携带的材料

列出不需要迁移的材料类型，例如：

- 已解决且无后续影响的中间日志；
- 已废弃方案的完整过程；
- 与当前阶段无关的旧聊天记录；
- 重复解释；
- 情绪化争执；
- 不影响下一步判断的临时尝试。

## 7. 新窗口读取顺序建议

根据实际输出列出：

1. 先读 `handoff-current.md`；
2. 再读 `handoff-evidence.md`；
3. 如果存在 `todolist.md`，再读 `todolist.md`；
4. 如果 `handoff-current.md`、`handoff-evidence.md`、`todolist.md` 之间冲突，先指出冲突，不要自行合并。
```
